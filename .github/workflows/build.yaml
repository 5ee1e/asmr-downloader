name: npm run build

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: setup Node.JS
        with: 
          node-version: latest
      - run: npm ci
      - name: start build
      - run: npm run plug
